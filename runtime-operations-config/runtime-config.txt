# Runtime / launcher settings
# Set only the pieces that differ on your machine.
# The launcher scripts derive BASE_DIR and WORKDIR automatically.

CONDA_BIN=conda
CONDA_ENV=fastai
PYTHON_ENTRY=agent.py
MONITOR_ENTRY=monitoring/monitor.py
